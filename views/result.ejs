<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            font-family: 'Poppins','sans-serif';
            box-sizing: border-box;
        }

        body{
            background-image: url(../assets/icons/background.jpg);
        }

        .app{
            background: #fff;
            padding: 30px;
            width: 90%;
            max-width: 600px;
            margin: 100px auto 0;
            border-radius: 10px;
        }
        .app h1{
            font-size: 25px;
            color: #001e4d;
            font-weight: 600;
            border-bottom: 1px solid #333;
            padding-bottom: 30px;

        }

        .quiz{
            padding: 20px 0;
        }
        p{
            font-size: 18px;

        }
        button{
            background: #001e4d;
            color: #fff;
            font-weight: 500;
            width: 150px;
            border: 0;
            padding: 10px;
            margin: 20px auto 0;
            border-radius: 4px;
            cursor: pointer;
            display: block;
        }
    </style>
</head>
<body>
    <div class="app">
        <h1>Result</h1>
        <div class="quiz">
            <p>You scored <%= score %> out of <%= length %>!</p>
            <form action="/backtohome" method="/get">
                <button id="next-question" type="submit" name="chosenOption">Back to home</button>
            </form>
        </div>
    </div>
</body>
</html>